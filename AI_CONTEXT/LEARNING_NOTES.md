# LEARNING_NOTES.md
Concepts learned through actually implementing things in this project — not
general theory dumps. Add an entry after a concept was taught and used, not
before. Keep entries short: what the problem was, what the concept is, how
it was used here.

Format per entry:
```
## <Concept>
**Problem it solves here:**
**Core idea:**
**Where used in this project:**
**Trade-offs / production considerations:**
```

---

(No entries yet — implementation hasn't started. First entries will likely
come from building the Python RabbitMQ consumer: async consumer patterns
with `aio_pika`, manual ack/nack, and — once decided — TTL-based retry
queues and Dead Letter Queues as a real system-design pattern.)
